package utilities

import (
// "fmt"
)

func Commit() {
	ParseCliArgs()
	/*
		commitType := GetCommitType()
		scope := GetScope()
		message := GetCommitMessage()

		fmt.Println(commitType, scope, message)
	*/
}
